var util = require('util');
var vm = require('vm');

var sandbox = { };

Object.keys(global).forEach(function (key) {
  sandbox[key] = global[key];
});

vm.createContext(sandbox);

for (var i = 0; i < 10; ++i) {
  vm.runInContext('setTimeout(function() {console.log("foo"); }, 1000)', sandbox);
}
